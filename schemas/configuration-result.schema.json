{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": [
    "format",
    "schema_version",
    "status",
    "message"
  ],
  "additionalProperties": false,
  "properties": {
    "format": {
      "const": "project-cbm.config-result"
    },
    "schema_version": {
      "const": 1
    },
    "status": {
      "enum": [
        "saved_restart",
        "credentials_required",
        "ok",
        "invalid",
        "pending",
        "unavailable",
        "failed",
        "busy",
        "saved_pending",
        "wifi_failed",
        "wifi_country_required",
        "wifi_scan_unconfirmed"
      ]
    },
    "message": {
      "enum": [
        "Keyboard layout saved. It applies after reboot; current console input is unchanged.",
        "Set a separate File Sharing password before turning it on.",
        "Setting applied.",
        "The value is not supported. Check the setting and try again.",
        "Complete local first-boot setup before changing this setting.",
        "The required system facility is unavailable. Review System Information or Advanced guidance.",
        "The operation could not be confirmed. Review current state before retrying; part of it may have applied.",
        "Another configuration operation is running. Try again when it finishes.",
        "Settings saved; runtime integration is pending. The service was not enabled.",
        "Could not connect to Wi-Fi. Check the password, signal and router settings, then retry or choose another network. Authentication failure was not separately identified.",
        "Set the Wi-Fi country where this Pi is used before connecting.",
        "Wi-Fi readiness or scan completion could not be confirmed within 25 seconds. Retry, check the radio/country settings, or stay offline."
      ]
    }
  }
}
