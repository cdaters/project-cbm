# Product-owned content share. Owner authentication maps writes to appliance content ownership.
[Project CBM]
    path = /home/pi/pcbm
    browseable = yes
    read only = no
    guest ok = no
    valid users = owner
    force user = pi
    follow symlinks = no
    wide links = no
