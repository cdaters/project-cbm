# Fragment for a future reviewed Samba configuration; not installed by this pass.
# Include only after authenticated SMB enrollment; replace fixed account if needed.
[Project CBM]
    path = /home/pi/pcbm
    browseable = yes
    read only = no
    guest ok = no
    valid users = pi
    follow symlinks = no
    wide links = no
