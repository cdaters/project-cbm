--- a/export-image/01-user-rename/01-run.sh
+++ b/export-image/01-user-rename/01-run.sh
@@ -2,8 +2,2 @@
-
-if [[ "${DISABLE_FIRST_BOOT_USER_RENAME}" == "0" ]]; then
-	on_chroot <<- EOF
-		SUDO_USER="${FIRST_USER_NAME}" rename-user -f -s
-	EOF
-else
-	rm -f "${ROOTFS_DIR}/etc/xdg/autostart/piwiz.desktop"
-fi
+# CBM owns offline account initialization; no upstream rename on first boot.
+rm -f "${ROOTFS_DIR}/etc/xdg/autostart/piwiz.desktop"
--- a/export-image/02-set-sources/01-run.sh
+++ b/export-image/02-set-sources/01-run.sh
@@ -2 +2 @@
-
+# Frozen CBM stages already resolved and installed the complete package set.
@@ -7,2 +6,0 @@
-apt-get update
-apt-get -y dist-upgrade --auto-remove --purge
--- a/export-image/05-finalise/01-run.sh
+++ b/export-image/05-finalise/01-run.sh
@@ -55,0 +56,2 @@
+
+rm -f "${ROOTFS_DIR}/usr/sbin/policy-rc.d"
